/*
Задача 2: 
Дан массив ["a", "b", "c", "d"].
Используя join, создайте строку, где элементы массива разделены символом ``.
*/

const letters = ["a", "b", "c", "d"];

const concatenatedString = letters.join("``");

console.log(concatenatedString);